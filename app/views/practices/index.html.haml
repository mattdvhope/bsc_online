%section.site_title
  %header
    %h2 You can practice this lesson in three ways:
    -@practices.each do |practice|
      %h3=link_to practice.type_of, part_lesson_practice_path(@lesson.part, @lesson, practice)
<header class="header">

  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "City English", root_path, :class => "navbar-brand" %>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
          <!-- <li
            style="padding: 1em; padding-top: 1.1em;"
            class="fb-like"
            data-share="true"
            data-width="450"
            data-show-faces="true">
          </li> -->
          <!-- <li style="padding: 1em; padding-top: 1.1em;"><div class="line-it-button" style="display: none;" data-type="share-a" data-lang="en"></div></li> -->

          <% if !current_user %>
            <li><a href="#page-top" id="place_1" class="page-scroll">Top</a></li>

            <!-- <li><a href="/auth/facebook">Facebook</a></li> -->

            <% if params[:action] == 'front' %>
              <li><a href="#features" id="place_2" class="page-scroll">Features</a></li>
            <% elsif params[:action] == 'volunteer_intro' %>
              <li><a href="#features" id="place_2" class="page-scroll">Volunteer Steps</a></li>
            <% end %>

            <% if params[:action] == 'front' %>
              <li><a href="#process" id="place_3" class="page-scroll">Process</a></li>
            <% end %>

            <li id="backbone-app">
              <%= link_to "Login", log_in_student_path unless current_user %>
            </li>


            <% if params[:action] == 'front' || 'volunteer_intro' %> <!-- !!!! SECURITY problem with string literal -->
              <li id="backbone-app">
                <%= link_to "Register", register_student_path unless current_user %>
              </li>
            <% end %>

          <% end %>

          <!-- <li class="dropdown">
            <a href="" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Register<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li role="separator" class="divider"></li>
            </ul>
          </li> -->
          <li>
            <%= link_to "Log Out", log_out_path if current_user %>
          </li>


        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>

</header>

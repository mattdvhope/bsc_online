.nested-fields
  %h3= f.input :answer_content
  %h2= f.label "Select whether this answer is 'Wrong' or 'Correct'?"
  -if f.object.choice == "Correct"
    %h2= f.select :choice, options_for_select(["Correct", "Wrong"])
  -else
    %h2= f.select :choice, options_for_select(["Wrong", "Correct"])
  %h3= link_to_remove_association "remove answer", f
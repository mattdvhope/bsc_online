- if answer.choices.count > 0
  - question.answers.each do |answer|

    - if answer.choices.where(student_id: current_user.id).first.selected == true
      = render 'assessments/choice_selected_true', :answer => answer
    - else
      = render 'assessments/choice_selected_false', :answer => answer

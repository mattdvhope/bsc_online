%div{ 'ng-app' => 'Raffler' }
  %div{:'ng-controller' => 'RaffleCtrl'}
    = form_tag("/", "ng-submit" => "addEntry($event)") do
      %label
        Entry
      %br
      %input{"ng-model" => "newEntry.name"}/
      %br
      %button{:type => 'submit'}
        Submit Entry
    %ul.subnav
      %li{:'ng-repeat' => "entry in entries | orderObjectBy: 'created_at'"}
        {{entry.name}}
        %span.winner{"ng-class" => "{highlight: entry == lastWinner}", "ng-show" => "entry.winner"} WINNER
    %button{"ng-click" => "drawWinner()"} Draw winner
    %br
    %button{"ng-click" => "removeWinner()"} Remove the winner
